to compile from the command line run configure.bat
it will create a build environment compatible with
the tools available on your system. Then run the
Build.bat file to compile. The dlls will appear in
the dist folder.